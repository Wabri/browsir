package utils

import (
	"os"
	"testing"
)

var HOME = os.Getenv("HOME")

func UtilsTest(t *testing.T) {
	t.Run("LoadLocalShortcuts", func(t *testing.T) {
		// TODO: Implement test for LoadLocalShortcuts
	})
}
